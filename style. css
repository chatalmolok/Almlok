/* style.css */
:root {
  --bg: #000;
  --panel: #111;
  --accent: gold;
  --muted: #222;
  --text-dark: #000;
}

* { box-sizing: border-box; }
body {
  margin:0;
  font-family: 'Cairo', 'Tajawal', sans-serif;
  background: var(--bg);
  color: var(--accent);
  -webkit-font-smoothing:antialiased;
  -moz-osx-font-smoothing:grayscale;
}

/* container used in index/login/signup/reset */
.container {
  width:90%;
  max-width:420px;
  margin:40px auto;
  background: var(--panel);
  border:2px solid var(--accent);
  padding:24px;
  border-radius:12px;
  text-align:center;
  box-shadow: 0 0 18px rgba(212,175,55,0.12);
}

input {
  width:100%;
  padding:12px;
  margin:8px 0;
  border-radius:8px;
  border:1px solid var(--accent);
  background: var(--muted);
  color: var(--accent);
  outline:none;
  font-size:15px;
}

.btn {
  display:inline-block;
  width:100%;
  padding:12px;
  margin-top:10px;
  background: var(--accent);
  color: var(--text-dark);
  border:none;
  border-radius:8px;
  font-weight:700;
  cursor:pointer;
}

.btn:hover { opacity:0.95; }

a { color: var(--accent); display:block; margin-top:8px; text-decoration:none; }
a:hover { text-decoration:underline; }

.chat-header {
  display:flex;
  justify-content:space-between;
  align-items:center;
  padding:12px 16px;
  background:var(--panel);
  border-bottom:1px solid rgba(212,175,55,0.08);
}

.chat-main { height:calc(100vh - 180px); overflow-y:auto; padding:12px; }
.messages { display:flex; flex-direction:column; gap:10px; }

.message {
  background: #0e0e0e;
  border:1px solid rgba(212,175,55,0.12);
  padding:10px;
  border-radius:10px;
}

.chat-footer {
  position:fixed;
  bottom:10px;
  left:50%;
  transform:translateX(-50%);
  width:90%;
  max-width:700px;
  display:flex;
  gap:8px;
  background:transparent;
}

.chat-footer input {
  flex:1;
  border-radius:10px;
}

.small-btn {
  background: transparent;
  border:1px solid var(--accent);
  color: var(--accent);
  padding:6px 10px;
  border-radius:8px;
  cursor:pointer;
}